[project]
name = "remindspeak-research"
version = "0.1.0"
description = "Reproducible scratch-training pipeline for the grounded RemindSpeak surface generator."
requires-python = ">=3.11"
dependencies = ["numpy>=2.0,<3", "onnx>=1.22,<2"]

[tool.setuptools]
py-modules = []

[tool.ruff]
line-length = 100
target-version = "py311"
